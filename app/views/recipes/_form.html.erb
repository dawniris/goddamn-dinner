  <%= simple_form_for @recipe do |f| %>
    <%= f.input :name %>
    <%= f.input :infobox, input_html: { id: 'box' }%>
    <%= f.association :category, collection: Category.all, as: :check_boxes %>
    <%= f.button :submit %>
  <% end %>

  <%= link_to 'New Tag', new_category_path %>